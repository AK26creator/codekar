---
---
<section class="mission-vision-section">
  <div class="container">
    <div class="mv-grid">
      <!-- Vision Card -->
      <div class="mv-card parallax-mv" data-speed="0.3">
        <div class="mv-icon">
          <svg xmlns="http://www.w3.org/2000/svg" width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M2 12s3-7 10-7 10 7 10 7-3 7-10 7-10-7-10-7Z"/>
            <circle cx="12" cy="12" r="3"/>
          </svg>
        </div>
        <h4>VISION</h4>
        <h3>Our Vision</h3>
        <p>
          To create meaningful opportunities for students by offering hands-on practical 
          experience, real-world project exposure, and internship opportunities with reputed 
          corporate institutions—preparing them for industry-ready careers.
        </p>
      </div>

      <!-- Mission Card -->
      <div class="mv-card parallax-mv" data-speed="0.5">
        <div class="mv-icon">
          <svg xmlns="http://www.w3.org/2000/svg" width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <circle cx="12" cy="12" r="10"/>
            <path d="M12 2a14.5 14.5 0 0 0 0 20 14.5 14.5 0 0 0 0-20"/>
            <path d="M2 12h20"/>
          </svg>
        </div>
        <h4>MISSION</h4>
        <h3>Our Mission</h3>
        <p>
          To build a future-ready student ecosystem that encourages innovation, skill 
          development, and real-world impact through technology and industry collaboration.
        </p>
      </div>
    </div>
  </div>
</section>

<script>
  // Parallax effect for Mission/Vision cards
  const mvCards = document.querySelectorAll('.parallax-mv');
  
  const handleMVScroll = () => {
    mvCards.forEach(card => {
      const rect = card.getBoundingClientRect();
      const speed = parseFloat(card.getAttribute('data-speed') || '0.3');
      const scrollProgress = (window.innerHeight - rect.top) / window.innerHeight;
      
      if (scrollProgress > 0 && scrollProgress < 1.5) {
        const offset = (scrollProgress - 0.5) * 30 * speed;
        card.style.transform = `translateY(${offset}px)`;
      }
    });
  };
  
  window.addEventListener('scroll', handleMVScroll, { passive: true });
  handleMVScroll();
</script>

<style>
  .mission-vision-section {
    padding: 6rem 0;
    background: #f8fafc;
  }

  .mv-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(400px, 1fr));
    gap: 2.5rem;
    max-width: 1200px;
    margin: 0 auto;
  }

  .mv-card {
    background: white;
    padding: 3rem;
    border-radius: 1.5rem;
    border: 1px solid #e2e8f0;
    transition: all 0.3s ease;
    position: relative;
  }

  .mv-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 20px 40px -10px rgba(37, 99, 235, 0.15);
    border-color: var(--color-s-primary);
  }

  .mv-icon {
    width: 64px;
    height: 64px;
    background: rgba(37, 99, 235, 0.1);
    border-radius: 1rem;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 1.5rem;
    color: var(--color-s-primary);
  }

  .mv-card h4 {
    font-size: 0.875rem;
    font-weight: 600;
    letter-spacing: 0.15em;
    color: var(--color-s-primary);
    margin-bottom: 0.5rem;
  }

  .mv-card h3 {
    font-size: 1.75rem;
    font-weight: 700;
    color: var(--color-s-text);
    margin-bottom: 1rem;
  }

  .mv-card p {
    color: #64748b;
    line-height: 1.8;
    font-size: 1rem;
  }

  @media (max-width: 768px) {
    .mv-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
